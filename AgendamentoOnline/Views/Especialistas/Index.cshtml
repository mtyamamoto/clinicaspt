@model IEnumerable<AgendamentoOnline.Models.Especialista>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EspecialistaNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CRM)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especialidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EspecialistaNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CRM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Especialidade.EspecialidadeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EspecialistaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EspecialistaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EspecialistaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
